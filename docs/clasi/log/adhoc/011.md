---
timestamp: '2026-03-20T20:20:28'
parent: team-lead
child: todo-worker
scope: docs/clasi/todo/
result: success
files_modified:
- docs/clasi/todo/agent-dispatch-templates-with-role-and-field-conventions.md
---

# Dispatch: team-lead → todo-worker

You are the todo-worker. Your scope is docs/clasi/todo/.

The stakeholder said:

"Let's create templates that each level of agent will use to give to the next level of agent. Rather than having the team lead make up a form each time for the sprint executor or the sprint planner, we will create a template for it. The template won't be a formal template like a Jinja template. It'll be one that uses our own templating language. The instructions, which will be real simple, right? The instructions will say, 'Go get this template and then fill in the fields that are in uppercase.'

For instance, one thing the templates should have is explicit instructions on what the types of subagent the receiver of the dispatch is. And we should encode additional instructions about what that agent should do into those files."

Create a TODO from this raw stakeholder input.

Working directory: /Volumes/Proj/proj/RobotProjects/scratch/claude-agent-skills

Commit when done.
