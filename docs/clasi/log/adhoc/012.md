---
timestamp: '2026-03-20T21:47:12'
parent: team-lead
child: todo-worker
scope: docs/clasi/todo/
result: success
files_modified:
- docs/clasi/todo/enforce-dispatch-template-usage.md
---

# Dispatch: team-lead → todo-worker

You are the todo-worker. Your scope is docs/clasi/todo/.

The stakeholder said:

"It does not look, from the e2e test, that the system is using the dispatch templates. The dispatches are still ad-hoc prompts, not filled-in templates. The dispatch templates exist in the agent directories but the agent doesn't actually load and use them. The templates are instructional again — they exist, the agent is told to use them, but it doesn't."

Create a TODO about this. The key issues are:
1. Templates exist but aren't being used in practice
2. Nothing mechanically forces the agent to use them
3. Possible fix: an MCP tool like `get_dispatch_template(target_agent)` that returns the template text, which the agent fills in
4. The `log_subagent_dispatch` tool could require `template_used` for dispatches to templated agents
5. The e2e verify script should check for `template_used` in dispatch log frontmatter

Working directory: /Volumes/Proj/proj/RobotProjects/scratch/claude-agent-skills

Commit when done.
