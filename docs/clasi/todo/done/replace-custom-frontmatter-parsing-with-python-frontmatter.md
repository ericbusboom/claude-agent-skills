---
status: done
sprint: '002'
tickets:
- 008
---

# Replace Custom Frontmatter Parsing With python-frontmatter Module

The project has custom YAML frontmatter parsing/writing code. This should probably be replaced by the `python-frontmatter` module, which handles reading, modifying, and writing YAML frontmatter in markdown files as a well-tested, maintained library.
