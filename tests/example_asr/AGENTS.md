# Example clasr provider

This block is the top-level guidance that `clasr` injects into the
target's `AGENTS.md`, `CLAUDE.md`, and `.github/copilot-instructions.md`.

The agent should:

- Run linters before committing.
- Prefer small focused commits over large ones.
- Read `SKILL.md` files under `skills/` for repeatable workflows.
